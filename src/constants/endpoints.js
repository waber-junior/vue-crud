const baseApiUrl = "http://127.0.0.1:8000/api";

export const ENDPOINTS_GROUP = {
    data(){
        return {
            ENDPOINTS: {
                users: baseApiUrl + "/users"
            }
        };
    }
};