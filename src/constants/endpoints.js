const baseApiUrl = "http://127.0.0.1:8080";

export const ENDPOINTS_GROUP = {
    data(){
        return {
            ENDPOINTS: {
                students: baseApiUrl + "/students"
            }
        };
    }
};